Before we continue to BF002.2, please update the Contracts layer with the following architecture changes.

1. CrudRepositoryInterface
- Rename findById() to find().
- Accept int|string as the identifier.
- Replace findAll() with all(array $filters = []): array.

2. TransactionInterface
- Add:
  public function transaction(callable $callback);

3. ValidatorInterface
- Replace the current contract with:
  - validate(array $data): array
  - rules(): array
  - errors(): array

4. LoggerInterface
- Remove log().
- Keep only:
  - info()
  - warning()
  - error()

5. CacheInterface
- Rename set() to put().
- Rename delete() to forget().

Update only the interfaces.
Do not add any implementations.
When finished, stop and provide the diff for review.