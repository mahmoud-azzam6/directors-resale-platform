'use client';
import { useEffect, useMemo, useState } from 'react';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { ApiClientError } from '@/lib/api/client'; import { propertyApi } from '@/lib/api/property'; import type { SaveProfileInput } from '@/types/property';
import { Alert } from '@/components/ui/alert'; import { Button } from '@/components/ui/button'; import { Card, CardContent, CardHeader } from '@/components/ui/card'; import { Input } from '@/components/ui/input'; import { Label } from '@/components/ui/label';
const value=(event:React.ChangeEvent<HTMLSelectElement>)=>event.target.value===''?null:Number(event.target.value);
const title=(item:{name_en:string;name_ar:string})=>item.name_ar||item.name_en;
export function PropertyDataPage({id}:{id:string}){const qc=useQueryClient();const [category,setCategory]=useState<number|null>(null);const [unitType,setUnitType]=useState<number|null>(null);const [geography,setGeography]=useState<number|null>(null);const [developer,setDeveloper]=useState<number|null>(null);const [measurementValues,setMeasurementValues]=useState<Record<number,string>>({});const [attributeValues,setAttributeValues]=useState<Record<number,string>>({});const [conflict,setConflict]=useState(false);
 const core=useQuery({queryKey:['property-core-form'],queryFn:propertyApi.coreForm});const profile=useQuery({queryKey:['property-profile',id],queryFn:()=>propertyApi.profile(id)});const projection=useQuery({queryKey:['property-form',unitType],queryFn:()=>propertyApi.unitTypeForm(unitType!),enabled:unitType!==null});
 useEffect(()=>{const p=profile.data?.profile;if(!p)return;setCategory(p.property_category_id);setUnitType(p.unit_type_id);setGeography(p.geographic_location_id);setDeveloper(p.developer_id);},[profile.data]);
 const unitTypes=useMemo(()=>core.data?.unit_types.filter((item)=>item.category_id===category)??[],[core.data,category]);
 const save=useMutation({mutationFn:(payload:SaveProfileInput)=>propertyApi.saveProfile(id,payload),onSuccess:async(data)=>{setConflict(false);await qc.invalidateQueries({queryKey:['property-profile',id]});if(data.profile){setCategory(data.profile.property_category_id);setUnitType(data.profile.unit_type_id);}},onError:async(error)=>{if(error instanceof ApiClientError&&error.code==='validation_error'&&error.fields?.PROFILE_REVISION_CONFLICT){setConflict(true);await qc.refetchQueries({queryKey:['property-profile',id]});}}});
 if(core.isLoading||profile.isLoading)return <main className="page-shell"><p className="text-sm text-muted">Loading Property Data...</p></main>;if(core.error||profile.error){const error=(core.error??profile.error) as ApiClientError;return <main className="page-shell"><Alert>{error?.status===403?'Your current backend permissions do not allow this Property.':error?.message??'Property Data could not be loaded.'}</Alert></main>}
 const submit=()=>{const measurements: NonNullable<SaveProfileInput['measurements']>=[];const attributes: NonNullable<SaveProfileInput['attributes']>=[];projection.data?.measurements.forEach((field)=>{const raw=measurementValues[field.definition_id];if(raw)measurements.push({definition_id:field.definition_id,value:raw,unit_code:field.unit})});projection.data?.attributes.forEach((field)=>{const raw=attributeValues[field.definition_id];if(raw===undefined||raw==='')return;if(field.data_type==='ENUM')attributes.push({definition_id:field.definition_id,option_id:Number(raw)});else if(field.data_type==='BOOLEAN')attributes.push({definition_id:field.definition_id,value:raw==='true'});else if(field.data_type==='INTEGER')attributes.push({definition_id:field.definition_id,value:Number(raw)});else attributes.push({definition_id:field.definition_id,value:raw})});const payload:SaveProfileInput={property_category_id:category,unit_type_id:unitType,measurements,attributes};if(geography!==null)payload.geographic_location_id=geography;if(developer!==null){payload.development_reference_type='DEVELOPER';payload.developer_id=developer};if(profile.data?.profile)payload.expected_revision=profile.data.profile.revision;save.mutate(payload)}; const apiError=save.error instanceof ApiClientError?save.error:null;return <main className="page-shell fade-up"><div><p className="eyebrow">Property setup · 1 of 3</p><h1 className="mt-2 text-3xl font-semibold text-ink">Property Data / بيانات الوحدة</h1><p className="mt-2 text-sm text-muted">Save progressively. Requiredness and Listing readiness are not calculated here.</p></div>{conflict&&<div className="mt-6"><Alert><div><strong>This Profile changed elsewhere.</strong><p className="mt-1">Your input is still on this page. Review the latest server data, then explicitly save again to retry.</p></div></Alert></div>}{save.error&&!conflict&&<div className="mt-6"><Alert>{apiError?.message??'Property Profile could not be saved.'}</Alert></div>}<Card className="mt-8"><CardHeader><h2 className="font-semibold text-ink">Canonical selections</h2></CardHeader><CardContent><div className="grid gap-5 md:grid-cols-2"><div><Label htmlFor="category">Property Category</Label><select id="category" className="h-12 w-full rounded-md border border-line bg-surface px-3 text-sm" value={category??''} onChange={(e)=>{setCategory(value(e));setUnitType(null)}}><option value="">Select category</option>{core.data?.categories.map((item)=><option key={item.id} value={item.id}>{title(item)}</option>)}</select></div><div><Label htmlFor="unit">Unit Type</Label><select id="unit" disabled={!category} className="h-12 w-full rounded-md border border-line bg-surface px-3 text-sm disabled:opacity-60" value={unitType??''} onChange={(e)=>setUnitType(value(e))}><option value="">Select Unit Type</option>{unitTypes.map((item)=><option key={item.id} value={item.id}>{title(item)}</option>)}</select></div><div><Label htmlFor="geography">Geography</Label><select id="geography" className="h-12 w-full rounded-md border border-line bg-surface px-3 text-sm" value={geography??''} onChange={(e)=>setGeography(value(e))}><option value="">No Geography selected</option>{core.data?.geography.countries.map((item)=><option key={item.id} value={item.id}>{title(item)}</option>)}</select><p className="mt-1 text-xs text-muted">Country is available from the bounded core form. Deeper hierarchy selection remains a backend-read integration follow-up.</p></div><div><Label htmlFor="developer">Development hierarchy</Label><select id="developer" className="h-12 w-full rounded-md border border-line bg-surface px-3 text-sm" value={developer??''} onChange={(e)=>setDeveloper(value(e))}><option value="">No Development selected</option>{core.data?.development.developers.map((item)=><option key={item.id} value={item.id}>{title(item)}</option>)}</select><p className="mt-1 text-xs text-muted">Developer selection uses the implemented BF015 reference contract.</p></div></div></CardContent></Card>{unitType&&projection.isLoading&&<p className="mt-6 text-sm text-muted">Loading the Unit Type form...</p>}{projection.error&&<div className="mt-6"><Alert>{(projection.error as ApiClientError).message??'Dynamic form could not be loaded.'}</Alert></div>}{projection.data&&<Card className="mt-6"><CardHeader><h2 className="font-semibold text-ink">Unit Type fields</h2></CardHeader><CardContent><div className="grid gap-5 md:grid-cols-2">{projection.data.measurements.map((field)=><div key={field.definition_id}><Label htmlFor={`m-${field.definition_id}`}>{title(field)} ({field.unit})</Label><Input id={`m-${field.definition_id}`} inputMode="decimal" value={measurementValues[field.definition_id]??''} onChange={(e)=>setMeasurementValues({...measurementValues,[field.definition_id]:e.target.value})}/></div>)}{projection.data.attributes.map((field)=><div key={field.definition_id}><Label htmlFor={`a-${field.definition_id}`}>{title(field)}</Label>{field.data_type==='ENUM'?<select id={`a-${field.definition_id}`} className="h-12 w-full rounded-md border border-line bg-surface px-3 text-sm" value={attributeValues[field.definition_id]??''} onChange={(e)=>setAttributeValues({...attributeValues,[field.definition_id]:e.target.value})}><option value="">Select option</option>{field.options.map((option)=><option key={option.id} value={option.id}>{title(option)}</option>)}</select>:field.data_type==='BOOLEAN'?<select id={`a-${field.definition_id}`} className="h-12 w-full rounded-md border border-line bg-surface px-3 text-sm" value={attributeValues[field.definition_id]??''} onChange={(e)=>setAttributeValues({...attributeValues,[field.definition_id]:e.target.value})}><option value="">Select value</option><option value="true">Yes</option><option value="false">No</option></select>:<Input id={`a-${field.definition_id}`} type={field.data_type==='DATE'?'date':field.data_type==='INTEGER'||field.data_type==='DECIMAL'?'number':'text'} value={attributeValues[field.definition_id]??''} onChange={(e)=>setAttributeValues({...attributeValues,[field.definition_id]:e.target.value})}/>}</div>)}</div></CardContent></Card>}<div className="mt-7 flex items-center gap-4"><Button onClick={submit} disabled={save.isPending}>{save.isPending?'Saving...':'Save Property Data'}</Button>{profile.data?.profile&&<p className="text-xs text-muted">Profile revision {profile.data.profile.revision}</p>}</div></main>}